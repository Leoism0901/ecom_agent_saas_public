"""
Agent 专属 Prompt 模板常量模块

本模块集中管理 Agent 工作流中所有与大模型交互的 Prompt 模板，
是「Prompt 与业务逻辑分离」架构红线的核心保障。

设计原则（遵循项目 CLAUDE.md 工程规范）：
1. 所有 Prompt 模板以模块级常量形式定义，绝不硬编码在 Service 或 Node 业务逻辑中。
2. 每个 Prompt 常量必须配套完整中文注释，说明用途、调用节点、输入预期、输出强约束格式。
3. 新增 Prompt 仅需在本文件追加常量，业务工作流通过 import 引用，对上下游服务零侵入。
4. 输出格式强约束统一管控，统一限制大模型仅返回纯JSON，屏蔽多余文本、代码块、解释话术。
5. 领域抽取规则标准化，统一枚举约束、字段校验规则沉淀在Prompt注释内，统一AI输出规范。

架构价值：
- 解耦：大模型提示词与业务执行节点解耦，迭代Prompt无需改动工作流编排代码
- 可维护：全量提示词集中管理，统一迭代、统一格式校验、统一话术优化
- 可观测：每条Prompt绑定对应业务节点，便于日志溯源、效果灰度迭代、A/B测试
- 可复用：多节点共享通用抽取规则，避免重复编写提示词逻辑
"""

from typing import Final

# ============================================================
# 长记忆压缩 —— 对话标签提炼 Prompt
# 绑定工作流节点：summarize_memory 记忆压缩节点
# 业务场景：历史长对话分段压缩、结构化标签提取，用于向量库检索、会话摘要看板
# 输入预期：完整买家-客服对话上下文文本
# 输出强约束：单行标准JSON，固定3个字段，禁止额外文本、markdown、注释
# 输出结构定义：
# {
#   "summary": str 一句话核心诉求，上限50字符
#   "tags": list[str] 业务标签数组，固定3~5个标签维度
#   "emotion": str 固定枚举情绪值，仅4种可选
# }
# 枚举约束：emotion = ["平稳", "焦虑", "愤怒", "满意"]
# 标签抽取维度：订单标识、诉求类型、商品信息、售后阶段
# ============================================================
MEMORY_COMPRESSION_PROMPT: Final[str] = pass


# ============================================================
# 人工接管 —— 客诉工单信息抽取 Prompt
# 绑定工作流节点：human_fallback_node 人工兜底拦截节点
# 业务场景：AI无法自主处理高风险客诉时，自动抽取工单结构化数据推送人工客服工作台
# 输入预期：完整风险对话全量文本
# 输出强约束：单行标准JSON，固定3个字段，禁止额外文本、markdown、注释
# 输出结构定义：
# {
#   "order_id": str 订单编号，无则填充固定占位文本「未提供」
#   "issue": str 争议核心总结，上限80字符
#   "emotion_level": str 五级风险情绪枚举
# }
# 枚举约束：emotion_level = ["平稳", "急躁", "愤怒", "高危", "绝望"]
# 抽取规则：优先提取订单标识、商品问题、用户诉求、外部维权升级风险
# ============================================================
HUMAN_FALLBACK_PROMPT: Final[str] = pass