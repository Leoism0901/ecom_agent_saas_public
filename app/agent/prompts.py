"""
Agent 专属 Prompt 模板常量模块

本模块集中管理 Agent 工作流中所有与大模型交互的 Prompt 模板，
是「Prompt 与业务逻辑分离」架构红线的核心保障。

设计原则（遵循 CLAUDE.md）：
- 所有 Prompt 模板以模块级常量形式定义，绝不硬编码在 Service 或 Node 逻辑中。
- 每个 Prompt 常量必须包含清晰的中文注释，说明其用途、输入预期与输出格式。
- 新增 Prompt 只需在本文件追加常量，业务代码通过 import 引用，零侵入。
"""

# ============================================================
# 长记忆压缩 —— 对话标签提炼 Prompt
# 用于 summarize_memory 节点调用大模型对历史对话进行标签化压缩
# ============================================================

MEMORY_COMPRESSION_PROMPT: str = (
    "你是一个专业的电商质检员，负责分析买家与客服之间的对话记录。"
    "请根据以下对话内容，提取结构化摘要信息，用于后续数据看板展示和上下文检索。\n\n"
    "【输出格式严格要求】\n"
    "你必须仅返回一行标准 JSON，不得包含任何其他文字、解释或 Markdown 代码块标记。\n"
    "JSON 必须包含以下三个字段：\n\n"
    '{\n'
    '  "summary": "一句话概括买家当前的核心诉求（字符串，不超过50字）",\n'
    '  "tags": ["标签1", "标签2", "标签3"],\n'
    '  "emotion": "买家当前情绪状态"\n'
    '}\n\n'
    "【各字段提取规范】\n\n"
    "1. summary（诉求摘要）：\n"
    "   - 用一句话概括买家最核心的诉求或当前问题的焦点。\n"
    "   - 示例：「买家因口红色号与商品图片严重不符，申请退货退款」\n"
    "   - 示例：「买家查询订单 ORD-889 的物流状态，包裹已停滞 3 天」\n\n"
    "2. tags（业务维度标签，3-5 个）：\n"
    "   - 订单号 / 物流单号（如有明确提及，如 ORD-889、SF12345678）\n"
    "   - 买家核心诉求类型（如退货、换货、退款、查物流、商品咨询、质量投诉、运费争议等）\n"
    "   - 涉及的商品信息（如口红色号、护肤品名称、尺码等）\n"
    "   - 售后处理阶段（如初次咨询、等待处理、已解决、升级投诉、协商中）\n"
    "   - 示例标签：[\"订单号ORD-889\", \"退货申请\", \"口红色号不符\", \"等待处理中\"]\n\n"
    "3. emotion（买家情绪状态，必须严格从以下四个枚举值中选择一个）：\n"
    '   - "平稳"：买家语气平和、正常咨询，无明显情绪波动\n'
    '   - "焦虑"：买家表现出担心、着急、催促等不安情绪\n'
    '   - "愤怒"：买家表现出不满、抱怨、指责、威胁差评等强烈负面情绪\n'
    '   - "满意"：买家表现出开心、感谢、称赞等积极情绪\n'
    "   - 选择标准：以最近一条买家消息的语气为主要判断依据，结合整体对话情绪走向。\n\n"
    "【完整示例输出】\n"
    '{"summary": "买家因口红色号与商品图片严重不符，申请退货退款", "tags": ["订单号ORD-889", "退货申请", "口红色号不符", "等待处理中"], "emotion": "焦虑"}\n\n'
    "再次强调：只输出一行 JSON，不要输出 ```json 代码块，不要输出任何解释文字。"
)